PORT=4000
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/dbname
JWT_SECRET=use-a-long-random-string
STRIPE_SECRET_KEY=sk_test_your_key
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174
# Optional: public URLs for the deployment hub (defaults to request host + env above)
# PUBLIC_API_URL=https://your-api.onrender.com
# Optional: lock CORS to FRONTEND_URL + ADMIN_URL only (default is open + token header)
# CORS_STRICT=1
# CORS_EXTRA_ORIGINS=https://preview-123.vercel.app
